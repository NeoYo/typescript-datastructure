{
  "name": "meaningful",
  "version": "0.0.1",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "typescript": "^3.0.1"
  },
  "devDependencies": {
    "ava": "^1.0.0-beta.7",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0"
  },
  "ava": {
    "compileEnhancements": false,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "scripts": {
    "watch": "nodemon --watch 'src/**/*' -e ts,tsx --exec 'ts-node' ./src/index.ts",
    "test": "tsc && ava"
  }
}
